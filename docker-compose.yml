services:
  app:
    build: .
    image: vienna-apartment-scraper
    env_file: .env
    ports:
      - "${PORT:-5000}:3000"
    volumes:
      # SQLite database persists here.
      - ./data:/app/data
    restart: unless-stopped
